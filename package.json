{
  "name": "vgate-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Official product landing page and documentation for VGate — a self-hosted VLESS proxy management system.",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "deploy": "npm run docs:build"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
